{
	"name": "@hex/extension",
	"private": true,
	"scripts": {
		"esbuild": "esbuild ./src/extension.ts --bundle --outfile=../../dist/extension.js --external:vscode --format=cjs --platform=node",
		"build": "tsc -noEmit && npm run esbuild -- --minify",
		"watch": "npm run esbuild -- --sourcemap --watch"
	},
	"dependencies": {
		"ajv": "8.8.2",
		"clear-module": "4.1.2"
	}
}
